import styled from "styled-components";

export const LeftBarPageContainer = styled.div`
    font-size: 0.8rem;
    padding: 10px;
    cursor: pointer;

    & > svg {
        margin-left: 5px;
        position: relative;
        top: 3px;
    }
`;